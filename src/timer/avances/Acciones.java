package timer.avances;

import spigot.file.configuration.FileConfiguration;

public class Acciones extends CrearEditarEliminar {

    public Acciones(String key, FileConfiguration config) {
        super(key, config);
    }
}
